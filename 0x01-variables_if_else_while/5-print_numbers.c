#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Prints all single digit numbers of base 10 starting from 0.
 *
 * Return: 0.
 */
int main(void)
{
	int numb;

	for (numb = 0; num < 10; numb++)
		printf("%d", numb);

	printf("\n");

	return (0);
}
